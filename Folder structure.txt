Historian/
├── docker-compose.yml
├── db/
└── simulator/
    ├── package.json
    ├── src/
    │   ├── db.js              # Postgres connection pool
    │   ├── prng.js            # Seedable random number generator
    │   ├── stateMachine.js    # Batch phase transitions & setpoints
    │   ├── seeder.js          # 90-day fast backfill script
    │   └── index.js           # Live 1s tick & 5s persistence loop

└── grafana/
    └── provisioning/
        └── datasources/
            └── timescale.yml